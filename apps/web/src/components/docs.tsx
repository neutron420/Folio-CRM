import type { ReactNode } from 'react';
import { DocsLayout as FumaDocsLayout } from 'fumadocs-ui/layouts/docs';

export const DocsLayout = (props: any): ReactNode => {
  return <FumaDocsLayout {...props} />;
};
